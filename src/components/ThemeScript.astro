---
// 테마 초기화 스크립트 - FOUC 방지 전용
// 최소한의 동기 코드만 실행
---

<script is:inline>
  // 즉시 실행으로 FOUC 방지
  {
    const theme =
      localStorage.getItem('theme') ||
      (window.matchMedia('(prefers-color-scheme: dark)').matches ? 'dark' : 'light');

    if (theme === 'dark') {
      document.documentElement.classList.add('dark');
    }
  }
</script>
